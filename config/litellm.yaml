# GENERATED - DO NOT EDIT.  Source: config/models.psd1
# Regenerate: scripts/gen-litellm.ps1  (also runs on `llm gen` and `llm serve`)

model_list:
  - model_name: planner
    litellm_params:
      model: openai/planner
      api_base: http://localhost:8080/v1
      api_key: sk-local
  - model_name: coder
    litellm_params:
      model: openai/coder
      api_base: http://localhost:8080/v1
      api_key: sk-local
  - model_name: chat
    litellm_params:
      model: openai/chat
      api_base: http://localhost:8080/v1
      api_key: sk-local
  - model_name: fim
    litellm_params:
      model: openai/fim
      api_base: http://localhost:8080/v1
      api_key: sk-local
  - model_name: embed
    litellm_params:
      model: openai/embed
      api_base: http://localhost:8080/v1
      api_key: sk-local
  - model_name: coder-pro
    litellm_params:
      model: openai/glm-4-flash
      api_base: https://open.bigmodel.cn/api/paas/v4
      api_key: os.environ/ZHIPU_API_KEY
  - model_name: chat-pro
    litellm_params:
      model: deepseek/deepseek-chat
      api_base: https://api.deepseek.com
      api_key: os.environ/DEEPSEEK_API_KEY
  - model_name: planner-pro
    litellm_params:
      model: deepseek/deepseek-reasoner
      api_base: https://api.deepseek.com
      api_key: os.environ/DEEPSEEK_API_KEY

litellm_settings:
  num_retries: 3
  request_timeout: 120
  # Enable Langfuse tracing: set langfuseEnabled = $true in config/user.psd1, then llm gen + llm litellm
  # Set LANGFUSE_PUBLIC_KEY and LANGFUSE_SECRET_KEY as environment variables (Settings → API Keys in Langfuse UI)

general_settings:
  master_key: sk-local   # dummy — proxy is local-only, no real auth needed
