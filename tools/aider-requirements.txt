# aider — its own venv (tools/venv-aider). Conflicts with open-webui's pins, so kept separate.
# Lock after a good install: tools\venv-aider\Scripts\pip freeze > tools\aider-requirements.lock
aider-chat
